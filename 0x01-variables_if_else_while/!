#include <stdio.h>

/**
 * main - function prints the entire alphabet in lower case
 * and a new line
 * Return : Always 0
 */
int main(void)
{
	char c;

	for (c = 'a'; c <= 'z'; ++c)
	{
		printf("%c", c);
	}
	printf("\n");
	return (0);
}
